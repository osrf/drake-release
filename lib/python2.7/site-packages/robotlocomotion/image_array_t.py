"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import robotlocomotion.image_t

import robotlocomotion.header_t

class image_array_t(object):
    __slots__ = ["header", "num_images", "images"]

    def __init__(self):
        self.header = robotlocomotion.header_t()
        self.num_images = 0
        self.images = []

    def encode(self):
        buf = BytesIO()
        buf.write(image_array_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        assert self.header._get_packed_fingerprint() == robotlocomotion.header_t._get_packed_fingerprint()
        self.header._encode_one(buf)
        buf.write(struct.pack(">i", self.num_images))
        for i0 in range(self.num_images):
            assert self.images[i0]._get_packed_fingerprint() == robotlocomotion.image_t._get_packed_fingerprint()
            self.images[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != image_array_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return image_array_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = image_array_t()
        self.header = robotlocomotion.header_t._decode_one(buf)
        self.num_images = struct.unpack(">i", buf.read(4))[0]
        self.images = []
        for i0 in range(self.num_images):
            self.images.append(robotlocomotion.image_t._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if image_array_t in parents: return 0
        newparents = parents + [image_array_t]
        tmphash = (0x3afa7aac7daa3b4e+ robotlocomotion.header_t._get_hash_recursive(newparents)+ robotlocomotion.image_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if image_array_t._packed_fingerprint is None:
            image_array_t._packed_fingerprint = struct.pack(">Q", image_array_t._get_hash_recursive([]))
        return image_array_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

