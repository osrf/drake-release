"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class residual_observer_state_t(object):
    __slots__ = ["utime", "num_joints", "joint_name", "residual", "gravity", "internal_torque", "foot_contact_torque"]

    def __init__(self):
        self.utime = 0
        self.num_joints = 0
        self.joint_name = []
        self.residual = []
        self.gravity = []
        self.internal_torque = []
        self.foot_contact_torque = []

    def encode(self):
        buf = BytesIO()
        buf.write(residual_observer_state_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qh", self.utime, self.num_joints))
        for i0 in range(self.num_joints):
            __joint_name_encoded = self.joint_name[i0].encode('utf-8')
            buf.write(struct.pack('>I', len(__joint_name_encoded)+1))
            buf.write(__joint_name_encoded)
            buf.write(b"\0")
        buf.write(struct.pack('>%df' % self.num_joints, *self.residual[:self.num_joints]))
        buf.write(struct.pack('>%df' % self.num_joints, *self.gravity[:self.num_joints]))
        buf.write(struct.pack('>%df' % self.num_joints, *self.internal_torque[:self.num_joints]))
        buf.write(struct.pack('>%df' % self.num_joints, *self.foot_contact_torque[:self.num_joints]))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != residual_observer_state_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return residual_observer_state_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = residual_observer_state_t()
        self.utime, self.num_joints = struct.unpack(">qh", buf.read(10))
        self.joint_name = []
        for i0 in range(self.num_joints):
            __joint_name_len = struct.unpack('>I', buf.read(4))[0]
            self.joint_name.append(buf.read(__joint_name_len)[:-1].decode('utf-8', 'replace'))
        self.residual = struct.unpack('>%df' % self.num_joints, buf.read(self.num_joints * 4))
        self.gravity = struct.unpack('>%df' % self.num_joints, buf.read(self.num_joints * 4))
        self.internal_torque = struct.unpack('>%df' % self.num_joints, buf.read(self.num_joints * 4))
        self.foot_contact_torque = struct.unpack('>%df' % self.num_joints, buf.read(self.num_joints * 4))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if residual_observer_state_t in parents: return 0
        tmphash = (0x8c1b4e93b8978c7d) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if residual_observer_state_t._packed_fingerprint is None:
            residual_observer_state_t._packed_fingerprint = struct.pack(">Q", residual_observer_state_t._get_hash_recursive([]))
        return residual_observer_state_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

