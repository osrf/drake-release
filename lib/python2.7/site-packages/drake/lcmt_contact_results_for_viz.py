"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import drake.lcmt_contact_info_for_viz

class lcmt_contact_results_for_viz(object):
    __slots__ = ["timestamp", "num_contacts", "contact_info"]

    def __init__(self):
        self.timestamp = 0
        self.num_contacts = 0
        self.contact_info = []

    def encode(self):
        buf = BytesIO()
        buf.write(lcmt_contact_results_for_viz._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qi", self.timestamp, self.num_contacts))
        for i0 in range(self.num_contacts):
            assert self.contact_info[i0]._get_packed_fingerprint() == drake.lcmt_contact_info_for_viz._get_packed_fingerprint()
            self.contact_info[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != lcmt_contact_results_for_viz._get_packed_fingerprint():
            raise ValueError("Decode error")
        return lcmt_contact_results_for_viz._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = lcmt_contact_results_for_viz()
        self.timestamp, self.num_contacts = struct.unpack(">qi", buf.read(12))
        self.contact_info = []
        for i0 in range(self.num_contacts):
            self.contact_info.append(drake.lcmt_contact_info_for_viz._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if lcmt_contact_results_for_viz in parents: return 0
        newparents = parents + [lcmt_contact_results_for_viz]
        tmphash = (0x2d284a8cdbb2b74e+ drake.lcmt_contact_info_for_viz._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if lcmt_contact_results_for_viz._packed_fingerprint is None:
            lcmt_contact_results_for_viz._packed_fingerprint = struct.pack(">Q", lcmt_contact_results_for_viz._get_hash_recursive([]))
        return lcmt_contact_results_for_viz._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

